# Employee Management App

React + FastAPI + Postgres app that runs in Docker.

## Login

- Username: `admin`
- Password: `admin`

## Run

```bash
docker compose up --build
```

- Frontend: http://localhost:3000
- Backend health: http://localhost:8000/api/health
